// System-wide business rule constants. Change here to apply everywhere.

/** Maximum outbound messages (first messages + bumps + AI replies) sent per campaign per day. */
export const DEFAULT_DAILY_LEAD_LIMIT = 500;
