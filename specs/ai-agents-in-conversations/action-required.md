# Action Required: AI Agents in Conversations

No manual steps required.

All backend infrastructure (tables, seeded agents, API endpoints, CLI wrapper) is already live.
Implementation is purely frontend changes.
