// TODO: replace with a real <Helmet> or <head> meta tag implementation for your framework
export default function Seo({ title, description }: { title?: string; description?: string }) {
  return null;
}
