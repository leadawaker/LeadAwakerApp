// Re-exports the real, actively-maintained legal content from the main
// app's locale files so the legacy-styled page never drifts out of sync.
import content from "../../../locales/nl/privacyPolicy.json";
export default content;
